import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  themeController: {
    position: 'absolute',
    bottom: 30,
    right: 30,
    borderRadius: 24,
    padding: 8,
  },
});
